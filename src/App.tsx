// FileUploader is the primary UI — production-level design with embedding status,
// pipeline logs, backend health, and file change detection
import FileUploader from "./FileUploader";

export default function App() {
  return <FileUploader />;
}
